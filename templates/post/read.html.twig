{% extends "layout.html.twig" %}

{% block content %}
    <div class="container"> 
		<table class="table">
			<thead>
				<tr> 
					<th>Onderwerp</th>
					<th>{{ post.title }}</th>
				</tr>
			</thead>
			<tbody>
			<tr>
        		<td><a href="/author/{{ post.author_id }}">{{ post.name }} </a><br />{{ post.created_at }}</td>
        		<td>{{ post.text }}</td>
        	</tr>
			</tbody>
		</table>  
	</div>
{% endblock %}